---
layout: post
title:  "在屏幕顺序显示a-z（二）"
date:   2019-08-28 11:13
categories: 《汇编语言》
tags: Assembly 王爽
excerpt: 文中程序练习记录
author: Pythonbug
mathjax: true
---

## 延时程序如下
```
mov dx,10h  # 100000h
mov ax,0
s:  sub ax,1
    sbb dx,0
    cmp ax,0
    jne s
    cmp dx,0
    jne s
```

## 完整程序
```
assume cs:code
stack segment
    db 128 dup(0)
stack ends

code segment
start:  mov ax,stack
        mov ss,ax
        mov sp,128

        mov ax,0b800h
        mov es,ax
        mov ah,'a'
    s:  mov es:[160*12+40*2],ah
        call delay
        inc ah
        cmp ah,'z'
        jna s

        mov ax,4c00h
        int 21h

delay:  push ax
        push dx
        mov dx,1000h
        mov ax,0
    s1: sub ax,1
        sbb dx,0
        cmp ax,0
        jne s1
        cmp dx,0
        jne s1
        pop dx
        pop ax
        ret

code ends
end start
```

## 小结
主要还是学会如何在汇编中去设置延时